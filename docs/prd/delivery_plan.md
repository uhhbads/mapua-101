# Deliverables and Next Actions

## Deliverables
- Executable for Windows (offline) with all assets bundled.
- Configuration for resolutions, filter rotation interval, and font or overlay settings.
- README with setup and run steps; consent signage text snippet.

## Next Actions
- Pick implementation stack (C++ DirectX or Electron/WebGPU) based on team skills.
- Assemble assets (dog ears and nose PNGs, Y2K frame plus LUT, custom frame, font).
- Prototype pipeline: camera capture to MediaPipe detection and mesh to single-pass GPU composite to LG TV output.
- Benchmark on target hardware; adjust resolution and effects accordingly.
